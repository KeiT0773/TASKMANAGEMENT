# 🌐 Webアプリケーション開発 ITリテラシー理解度チェック

タスク管理アプリ **「TaskManagement」** の開発を通じて身につけるべき、

# 第1章 インターネットとネットワークの仕組み（12問）

## Q1. インターネットとは

「インターネット」と「Webブラウザで見るホームページ」は同じものですか？違うとすれば、どう違いますか？
**以下回答**
- インターネットとWebブラウザで見るホームページは違うものです。
　- インターネット：世界中のコンピュータをつなぐための通信インフラ
　- webブラウザで見るホームページ：ブラウザがURLを使ってWebサーバーへアクセスし、HTMLデータを受け取って画面に表示

---

## Q2. IPアドレスとポート番号

このプロジェクトではフロントエンドを `localhost:5173`、バックエンドを `localhost:8080` で起動しています。

- `localhost`とは何を指しますか？
- `:5173` や `:8080` のようなポート番号はなぜ必要ですか？
**以下回答**
- localhostは何を指すか：自分のPCのネットワーク上の名前
- ポート番号はなぜ必要か：ポート番号はPC内の複数サービスを区別するために必要
---

## Q3. DNS

ブラウザのアドレスバーに

`https://www.example.com`

と入力してからページが表示されるまでに、DNSはどのような役割を果たしますか？

**以下回答**
- DNSの役割：人間が指定したドメイン名を対応するIPアドレスへ変換
---

## Q4. HTTPとは

HTTPとは何の略で、どのような役割を持つものですか？

「プロトコル」という言葉の意味も含めて説明してください。

**以下回答**
- HTTPの略とその役割： “HyperText Transfer Protocol” の略で、WebブラウザとWebサーバがデータをやり取りするための通信ルール（プロトコル）
- プロトコルの意味：器同士が通信するときに守るべき取り決め・手順
---

## Q5. HTTPとHTTPSの違い

HTTPとHTTPSの違いは何ですか？

なぜ現在のWebサイトはほぼすべてHTTPSを使用していますか？

**以下回答**
- HTTPとHTTPSの違い: HTTP は“暗号化なしの通信”、HTTPS は“SSL/TLS で暗号化された安全な通信”
-HTTPSを使う理由: 盗聴・改ざんを防ぐため
---

## Q6. リクエストとレスポンス

Webにおける「リクエスト」と「レスポンス」の関係を、日常生活の例えを使って説明してください。

**以下回答**
リクエスト：レストランでハンバーグを注文
レスポンス：ハンバーグの調理

---

## Q7. HTTPメソッド

このアプリのAPIでは以下を使っています。

- GET
- POST
- PUT
- DELETE
- PATCH

これらの「HTTPメソッド」とは何ですか？

日常生活の例えで、それぞれの違いを説明してください。

**以下回答**
- HTTPメソッド: 「そのURLのデータに対して何をしたいか」をサーバーへ伝えるための“動詞”
- APIの違いをスーパーで例えると
　- GET：棚の商品を“見る”
  - POST：新しい商品を“棚に追加する”
  - PUT：棚の商品を“丸ごと入れ替える”
  - DELETE：棚の商品を“捨てる”
  - PATCH：棚の商品を“一部だけ直す”
---

## Q8. HTTPステータスコード

サーバーからの返事には「ステータスコード」という3桁の数字が含まれます。

以下のコードはそれぞれ何を意味しますか？

- 200
- 201
- 400
- 404
- 500

**以下回答**
- 200 : リクエストが正常に成功した
- 201 : 新しいリソースが正常に作成された
- 400 : リクエスト内容が不正（パラメータ不足・形式不正など）
- 404 : 指定されたリソースが存在しない
- 500 : サーバー内部で予期しないエラーが発生

---

## Q9. JSONとは

フロントエンドとバックエンド間でデータをやり取りする際に「JSON」という形式が使われています。

- JSONとは何ですか？
- なぜ広く使われていますか？

**以下回答**
- JSON : 「人間にも機械にも読みやすい、データ交換のための標準フォーマット」
- JSONが使われる理由
  - 人間が読みやすい
  - JavaScript と相性が良い（Web の標準言語）
  - ほぼすべての言語で扱える（汎用性）
  - 軽量で高速
  - REST API の事実上の標準
  - 構造化データを表現しやすい

---

## Q10. APIとは

このアプリではフロントエンドがバックエンドのAPIを呼び出してデータを取得しています。

- APIとは何ですか？
- なぜ直接データベースにアクセスしないのですか？

**以下回答**
- API : ソフトウェア同士が“決められた方法”でやり取りするための インターフェース（操作方法と規則）
- フロントエンドがDBに直接アクセスしない理由
  - セキュリティ（DBを外部に晒すのは危険）
  - 権限管理・認証をバックエンドで集中管理できる
  - 内部構造を隠蔽でき、変更に強くなる
  - 複数クライアント（Web/アプリ）から同じ処理を使える

---

## Q11. CORSとは

フロントエンド（5173）からバックエンド（8080）にアクセスする際、

「CORS」という仕組みが関係します。

CORSとは何のためにありますか？

**以下回答**
- CORS:同一オリジンポリシーによる制限を、必要な場合だけ緩めるための仕組み
- 悪意あるサイトがユーザーのブラウザを使って勝手に別サイトの情報（銀行口座など）を読み取るのを防ぐため

---

## Q12. クライアントとサーバー

「クライアント」と「サーバー」とは何ですか？

このアプリの構成の中で、それぞれどれがクライアントでどれがサーバーですか？

- ブラウザ
- バックエンド
- データベース

**以下回答**
- クライアント : ユーザーPC
- サーバー : Taskmanagemantアプリを提供するフロントエンド、バックエンド、DB
- アプリ構成群のクライアントとサーバーは以下の通り
  - ブラウザ : クライアント
  - バックエンド : サーバー
  - データベース : サーバー

---

# 第2章 Webアプリケーションの構造（8問）

## Q13. フロントエンドとバックエンド

「フロントエンド」と「バックエンド」の違いを説明してください。

このアプリではそれぞれ何を担当していますか？

**以下回答**
- フロントエンド : ユーザーが見て触る部分
　- このアプリでの担当
    - 画面を描く
    - ユーザー操作受付
    - バックエンドにAPIで依頼

- バックエンド : 裏でデータを管理する部分
　- このアプリでの担当
    - APIの窓口
    - 業務ルールの実行（カードの登録・更新・移動・削除）
    - DBとの通信 

---

## Q14. 3層構造

このアプリは

- フロントエンド
- バックエンド
- データベース

の3層構造です。

なぜこのように役割を分けるのですか？

1つのプログラムですべてをやらない理由を説明してください。

**以下回答**
3層構造で役割を分ける理由 : 1 つのプログラムですべてをやると、ブラウザにデータを直接触らせる危険が生じ、少しの変更が全体に影響して、直すのも調べるのも難しくなるため

---

## Q15. データの流れ

ユーザーが「新しいタスクを作成」ボタンを押してから、

- データベースに保存される
- 画面に反映される

までの流れを順を追って説明してください。

**以下回答**
1. フロントエンドがタイトルの空欄チェックをしたあと、POST /api/cards でタイトル・優先度・リストを JSON にして送る
2. バックエンドの Controller が受け取り、入力チェックを行う
3. Service がリストの存在を確認し、新しいカードを加えてリスト内を優先度順に並べ直す
4. Repository を通じて DB に INSERT・UPDATE する
5. バックエンドが 201 Created と登録したカードを返す
6. フロントエンドは並び順の変化に対応するため、GET /api/cards?listId=... でそのリストのカードを取り直す
7. 取得結果で state を更新すると、React が再描画する

---

## Q16. エンドポイントとURL設計

このアプリには以下のAPIがあります。

- `/api/tasks`
- `/api/tasks/{id}`

このようなURL設計の考え方を説明してください。

`{id}` の部分は何ですか？

**以下回答**
- URL設計の考え方 : URL は「何を扱うか（リソース）」を名詞で表し、「どうするか」は HTTP メソッド（GET・POST・PUT・DELETE）で表す REST の考え方で設計
- `{id}` の部分 : パスパラメータで、実際のリクエストでは /api/cards/42 のようにカードの ID（DB が登録時に振る主キー）に置き換えて送る

---

## Q17. 同期処理と非同期処理

「同期処理」と「非同期処理」の違いを、日常生活の例えで説明してください。

なぜWeb通信では非同期処理が必要ですか？

**以下回答**
- 同期処理 : 頼んだ処理が終わるまで待ってから次に進む方式で、行列に並んでラーメンができるまでカウンターの前で待ち続けるようなもの
- 非同期処理 : 頼んだら待たずにほかのことを進め、終わった知らせを受けてから続きをする方式で、フードコートで呼び出しベルを受け取り、鳴るまで席で別のことをしているようなもの
- Web通信では非同期処理が必要な理由 : Web 通信は、ネットワークとサーバーを往復するため時間がかかり、いつ終わるかもわからない。そのため非同期にし、応答を待つ間も画面を操作できるようにしている。


## Q18. SPA（シングルページアプリケーション）

このアプリはReactで作られたSPAです。

従来のWebサイト（ページ遷移するもの）と何が違いますか？

**以下回答**
- 従来のWebサイト : リンクやボタンを押すたびにサーバーが新しい HTML を丸ごと作って返し、ブラウザはページ全体を読み込み直す
- SPA : 最初に HTML を 1 枚と JavaScript を読み込んだあとは、サーバーとは JSON のデータだけをやり取りし、ブラウザ上の JavaScript（React）が画面の必要な部分だけを書き換える

---

## Q19. 入力チェック（バリデーション）の多重防御

入力チェックはフロントエンドとバックエンドの両方で行うべきと言われています。

なぜ片方だけでは不十分なのですか？

**以下回答**
- フロントエンドだけでは不十分な理由 : フロントエンドのプログラム（JS）はユーザーのブラウザで動いています。ユーザーはそれを見ることも、止めることも、書き換えることもできるため
- バックエンドだけでは不十分な理由 : 間違いに気づくのが送信後になり、無駄な通信も発生して使い心地が悪いため
---

## Q20. エラーハンドリング

API呼び出しが失敗した場合（ネットワーク障害やサーバーエラーなど）、

アプリはどのように対処すべきですか？

**以下回答**
- 失敗を検知して種類を見分ける：fetch は 404 や 500 でも例外を投げないので、ステータスを確認して自分でエラーとして扱う。接続できない場合、サーバーエラー（500）、入力の誤り（400 など）を区別する
- ユーザーにわかる言葉で伝える：「サーバーに接続できません」「タイトルは必須です」など、何が起きたかを、失敗した操作の近くに表示する

---

# 第3章 データベースの基礎（8問）

## Q21. データベースとは

データベースとは何ですか？

ExcelやCSVで保存する方法と比べて何が優れていますか？

**以下回答**
- データベース： 整理されて保存されたデータそのもの
- ExcelやCSVと比べて優れている点
  - データの正しさ : データベースは、列ごとに型と制約を決めておき、それに合わないデータを拒否できる
  - 途中で失敗しても壊れない : 「全部成功するか、全部なかったことにするか」のどちらかにできる
  - 同時実行の制御 : PosgreSQLは同時に届いた読み書きを調整し、互いの変更を壊さないように処理可能
  - 必要なデータだけを、速く取り出せる : インデックスを作っておけばデータが増えても必要な行だけ早く見つけて返すことができる
  - データ同士の関係を表現できる : このアプリには lists（リスト）と cards（カード）の 2 つのテーブルがあり、カードの list_id がリストの id を指すという関係で結ばれている


---

## Q22. テーブル・行・列

以下をExcelのシートに例えて説明してください。

- テーブル
- 行（レコード）
- 列（カラム）

tasksテーブルで具体例も挙げてください。

**以下回答**
- テーブル : Excelの表に相当
  - 具体例 : カードの表、リストの表
- 行(レコード) : Excelの表の行に相当
　- 具体例 : カード１枚が１行になる
- 列(カラム) : Excelの表の項目に相当
  - 具体例 : id, title, description, due_dateの項目

---

## Q23. 主キー（PRIMARY KEY）

tasksテーブルの `id` は主キーです。

- 主キーとは何ですか？
- なぜ必要ですか？

**以下回答**
- 主キー : テーブルの中から、1 行を確実に 1 つだけ特定するための列
- なぜ必要か
  - 「どの 1 件か」を確実に指すため : タイトルや期限などは別のカードと同じ値になりうるので、それらでは 1 枚に決められない。主キーがあれば、編集・削除の対象を必ず 1 件に特定できる
  - 値が変わっても、同じものを指し続けるため : タイトルや所属リストは編集や移動で変わるが、主キーは変わらないので、同じカードを指し続けられる
  - 「カードの呼び名」として使うため : バックエンドの検索、フロントエンドの画面描画など、アプリ全体でカードを見分ける共通の呼び名として使える
  -  テーブル同士をつなぐため : ほかのテーブルから特定の行を参照するとき（外部キー）の参照先になる

---

## Q24. CRUD操作

CRUDとは何の略ですか？

それぞれがこのアプリのどの機能に対応するか説明してください。

- Create
- Read
- Update
- Delete

**以下回答**
- CRUDとは何の略か : データに対する基本の 4 操作 Create（作成）・Read（読み取り）・Update（更新）・Delete（削除） の頭文字をとったもの。SQL ではそれぞれ INSERT・SELECT・UPDATE・DELETE、REST API では主に POST・GET・PUT・DELETE に対応する
- Create：「＋ カードを追加」からカードを登録する（POST /api/cards）
- Read：ボードを開いたときにリストとカードを表示する（GET /api/lists、GET /api/cards）
- Update：カード詳細でタイトル・説明文・期限・優先度を編集する（PUT /api/cards/{id}）
- Delete：カード詳細の「カードを削除」でカードを削除する（DELETE /api/cards/{id}）

---

## Q25. データの制約

tasksテーブルでは

- `NOT NULL`
- `DEFAULT 'medium'`

が設定されています。

これらは何のためにありますか？

**以下回答**
- `NOT NULL` : 必須の項目を空にさせない
   - 必要な理由 : その項目が空だとデータとして成り立たない、または、アプリが正しく動かなくなるので、空の状態で保存させないため
- `DEFAULT 'medium'` : 省略されたときの初期値
   - 必要な理由 : 値が指定されなかったときに、妥当な値を DB が自動で入れるため

---

## Q26. インデックス

インデックスとは何ですか？

本の索引に例えて説明してください。

**以下回答**
- インデックスとは : テーブルのデータを速く探すために、DB が別に用意しておく「目次・索引」のようなデータ
- 本の索引に例えると : 500 ページの料理の本で、「卵」を使うレシピを探す場面を例に考えると、索引がない場合＝全件走査になるが、索引がある場合＝インデックス「た」を段を使った検索が可能

---

## Q27. トランザクション

トランザクションとは何ですか？

タスクを別カラムへドラッグ移動する操作で、なぜ重要ですか？

**以下回答**
- トランザクション : 複数のデータ操作を「ひとまとまりの仕事」として扱い、全部成功するか、全部なかったことにするかのどちらかにする仕組み
- 移動する操作での重要性 : カードを別のリストへドラッグ移動すると、DB では移動したカードの list_id と display_order に加えて、移動先で後ろにずれるカードと、移動元で詰めるカードの display_order も書き換える。つまり 1 回の移動で複数の行を更新する。もしトランザクションがなく途中で失敗すると、同じリストに表示順が同じカードが 2 枚できたり、表示順に抜けができたりして、ボードの並びが壊れたまま DB に残ってしまうため

---

## Q28. SQLとORM

- SQLとはどのようなものですか？
- ORMの役割とは何ですか？

**以下回答**
- SQL : データベースに「何をしてほしいか」を伝えるための専用の言語
- ORMの役割 : プログラムのオブジェクトとデータベースのテーブルの行を自動で対応づける役割。このアプリでは Spring Data JPA を使い、Card クラスを cards テーブルに対応づけている。CardRepository にメソッド名を宣言するだけで SQL が自動生成され、トランザクション内で変更したオブジェクトは自動で UPDATE される

---

# 第4章 開発プロセスとチーム開発（4問）

## Q29. バージョン管理（Git）

Gitなどのバージョン管理システムとは何ですか？

なぜ必要ですか？

使わないとどんな問題が起きますか？

**以下回答**
- バージョン管理システム : ファイルの変更の履歴を、「いつ・誰が・何を・なぜ変えたか」とともに記録し、いつでも過去の状態を見たり戻したりできるようにする仕組み
- なぜ必要か
   - 変更を加えて動かなくなったとき、動いていた状態に戻すため
   - コミットには変更の理由を理解するため
   - 新しい機能は、main から切ったブランチの上で作り、安全に試すため
   - 複数人で同時に作業できるようにするため(Git は各自の変更をマージし、同じ箇所をぶつかる形で変更していればコンフリクトとして知らせてれる)
- 使わないと起きる問題
  - 壊したときに元に戻せない
  - 最新のファイルが増えてどれが正しいかわからなくなる
  - 同時に編集した人の変更が上書きで消える
  - なぜそのコードになったのかわからない
  - 不具合がいつ入ったか追えない
  - PCが壊れたらすべて失う

---

## Q30. ブランチとPull Request

- ブランチとは何ですか？
- Pull Request の目的は何ですか？
- なぜ全員が main に直接変更しないのですか？

**以下回答**
- ブランチ : mainから枝分かれさせた、作業用の独立した流れ
- Pull Request の目的 : 「このブランチの変更を main に取り込んでください」と依頼し、取り込む前に確認するための場 
- なぜ全員が main に直接変更しないのか
  - main を「いつでも動く状態」に保つため
  - 確認を経ずに変更が入るのを防ぐため
  - 作業どうしがぶつからないようにするため
  - 何か起きたときに、戻しやすく、追いやすくするため

---

## Q31. コミットメッセージの重要性

例：

`feat: ログイン機能を追加`

良いコミットメッセージを書くことがなぜ重要ですか？

**以下回答**
- ミットメッセージは、あとから履歴を読む未来の自分やチームのメンバーに向けた説明であり、次の理由で重要
  - 履歴が読める : git log の一覧を見るだけで、いつ何が変わったのかがわかる
  - 探せる : 不具合の原因を調べるとき、メッセージで対象のコミットを絞り込める
  - 「なぜ」が残る : 差分からわかるのは「何を変えたか」だけで、「なぜ変えたか」は書き残さないと失われる
  - レビューしやすい : 変更の目的が先にわかれば、それに対して正しいかを判断できる
  - 機械でも扱える：feat, fix のように決まった形で書けば、変更履歴の自動生成などに使える

---

## Q32. なぜ技術的な仕組みを理解すべきか

AI時代においてコードはAIが書けるようになりました。

それでも

- ネットワーク
- データベース
- Webアプリケーションの仕組み

を理解する必要がある理由を、あなたの考えで述べてください。

**以下回答**
- 機能ブロックの設計はAIで十分書けるようになったが、大規模の開発になると全ての要件定義書を読み込んでAIでコード生成することは2026年の現状では困難である。つまり機能ブロック分けのアーキテクチャ設計はまだ人がやる領域として残っている。このため、我々エンジニアがネットワークやデータベース、Webアプリの仕組みを理解することは依然として重要だと考える。